{
  "name": "",
  "version": "",
  "scripts": {
    "build": "tsc",
    "build:production": "npm run prestart && npm prune --production",
    "watch": "tsc --w",
    "prestart": "npm run build",
    "xprestart": "npm run build && func extensions install",
    "start:host": "func start",
    "test": "echo \"No tests yet...\""
  },
  "description": "",
  "devDependencies": {
    "@azure/functions": "^3.0.0",
    "typescript": "^4.5.5"
  },
  "dependencies": {
    "@haibun/cli": "^1.1.3",
    "@haibun/core": "^1.2.2",
    "@haibun/domain-webpage": "^1.1.1",
    "@haibun/web-playwright": "^1.3.1",
    "durable-functions": "^1.5.4"
  }
}
